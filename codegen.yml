schema: http://localhost:3000/graphql
generates:
  ./SchemaFirst/generated-model.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      defaultMapper: any
      # mappers: 
        # User: ./index#userModel
